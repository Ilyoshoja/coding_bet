import React from "react";
import cls from "./warmup.module.scss";
interface WarmupProps {}

const Warmup: React.FC<WarmupProps> = () => {
  return <div className={cls.warmup}>
    
    </div>;
};

export default Warmup;
